<%= form_with(model: [@micropost, @micropost.comments.build]) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Create your reply..." %>
  </div>
  <%= f.submit "Reply", class: "btn btn-primary" %>
<% end %>